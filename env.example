# Database Configuration
# Get your DATABASE_URL from Neon (https://neon.tech)
DATABASE_URL="postgresql://username:password@host/database?sslmode=require"

# NextAuth Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here-generate-with-openssl"

# Sweego SMS Configuration (Optional - for SMS functionality)
# Sweego is a European GDPR-compliant SMS API
# Get your API key from https://app.sweego.io
SWEEGO_API_KEY="your-sweego-api-key"
SWEEGO_SENDER_ID="YourBrand"
# Optional: Custom API URL (defaults to https://api.sweego.io/v1)
# SWEEGO_API_URL="https://api.sweego.io/v1"

# Application URL (for production)
NEXTAUTH_URL="https://your-app.vercel.app"

